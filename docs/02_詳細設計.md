# 詳細設計

## Phase 1 — フロントエンドUI（`phase1_ui/`）

### ファイル構成

```
phase1_ui/
├── index.html   レイアウト・DOM骨格
├── style.css    ダークテーマ
└── app.js       状態管理・全ロジック
```

ビルドなし・依存ライブラリなし。`index.html` をブラウザで直接開いて動作する。

---

### アプリ状態（`app.js`）

```js
const state = {
  layout: [],              // 2D配列: null=通路 | 座席ID文字列
  seats: {},               // { [seatId]: { categoryId, ticketNo } }
  activeCategoryId: 'A',  // 現在選択中の区分ID
};
```

`layout` と `seats` は独立しており、`layout` は物理配置のみ、`seats` は割当状態のみを持つ。
この分離により「通路定義」と「区分割当」を混在させない。

---

### 座席区分（`CATEGORIES`）

`app.js` 冒頭の定数配列。区分の追加・変更はここだけ編集する。

```js
const CATEGORIES = [
  { id: 'A',     name: 'A区分',   color: '#e74c3c' },
  { id: 'B',     name: 'B区分',   color: '#3498db' },
  { id: 'C',     name: 'C区分',   color: '#2ecc71' },
  { id: 'D',     name: 'D区分',   color: '#f39c12' },
  { id: 'VIP',   name: 'VIP',     color: '#9b59b6' },
  { id: 'STAFF', name: 'スタッフ', color: '#1abc9c' },
];
```

> **注意**: `id` はJSONキーになるため、既存データがある状態で変更すると互換性が壊れる。

---

### 主要関数

| 関数 | 引数 | 説明 |
|------|------|------|
| `buildDefaultLayout()` | なし | 仮レイアウト（10行×12列、列3・8が通路）を生成。Excel実装後は削除予定 |
| `renderGrid()` | なし | `state.layout` からグリッドDOMを全再構築。`applySeatStyle` を各座席に適用 |
| `applySeatStyle(cell, seatId)` | DOM要素, 座席ID | `state.seats[seatId]` の状態に応じてセルの色・テキストを更新 |
| `onSeatClick(seatId, cell)` | 座席ID, DOM要素 | クリック時: `activeCategoryId` を `state.seats` に書き込み → `applySeatStyle` |
| `showContextMenu(x, y, seatId)` | 座標, 座席ID | 右クリックメニューを表示。区分変更・消去のオプションを動的生成 |
| `renderSummary()` | なし | `state.seats` を集計してサイドバーのカテゴリ別件数・合計を更新 |
| `assignTickets()` | なし | `layout` の行列順（上→下、左→右）に `{prefix}{3桁連番}` を採番 |
| `exportJSON()` | なし | `state` + `ticketConfig` をJSONファイルとしてダウンロード |
| `importJSON(file)` | Fileオブジェクト | JSONを読み込み `state.layout` / `state.seats` を上書き後、`renderGrid()` |

---

### UIレイアウト

```
┌─────────────────────────────────────────────────┐
│ ヘッダー: タイトル | 区分ボタン | アクションボタン │
├──────────────────────────────────┬──────────────┤
│                                  │ 集計パネル   │
│  【ステージ】                     │ (区分別件数) │
│                                  │              │
│  座席グリッド                     │ チケット     │
│  (行ラベル + セル)                │ 採番パネル   │
│                                  │              │
└──────────────────────────────────┴──────────────┘
```

グリッドは `CSS Grid` で描画。列数は `state.layout[0].length` から動的に決まる。

---

### 座席ID命名規則（現状・仮）

```
行ラベル(A-J) + 列番号(通路を除いてカウント)
例: A1, A2, A3, A4 ... (列3・8が通路のため列番号は通路をスキップ)
```

Excel実装後、このルールはExcel側の定義に準拠させる。

---

## Phase 2 — Python処理（`phase2_python/`）※未実装

### 予定ファイル構成

```
phase2_python/
├── excel_importer.py    ExcelファイルをJSONレイアウトに変換
├── excel_exporter.py    色付きExcelファイルを出力
├── pdf_exporter.py      座席表PDFを出力
├── server.py            Electron向けローカルHTTPサーバー（フェーズ3）
└── requirements.txt     openpyxl, reportlab
```

---

### Excel インポート（`excel_importer.py`）

詳細フォーマットは [`docs/03_Excelフォーマット解析.md`](03_Excelフォーマット解析.md) を参照。

**入力**: `大阪フォーマット.xlsx`

**出力**: `layout`（2D配列）+ `seats`（チャネル割当辞書）+ `categories`（凡例）を含むdict

```python
def parse_excel(filepath: str) -> dict:
    """
    戻り値:
      {
        "sheets": {
          "マーブルビーチサイド": {
            "layout": [[seatId, ...], ...],   # null=通路
            "seats": { seatId: { "categoryId": str, "ticketNo": null } }
          },
          ...
        },
        "categories": [{ "id": str, "name": str, "color": str }]
      }

    パース手順:
      1. 凡例行を走査 → チャネル名・色コード・枚数の辞書を生成
      2. 'ブロック'を含む行でブロック開始列を検出
      3. C列の'N列'パターンで列番号を取得
      4. 座席セル（値=整数）の色コードからチャネルを逆引き
      5. 座席ID = '{シート短縮名}_{ブロック名}_{列番号}列_{座席番号}番'
    """
```

**シート名の短縮マッピング**:

| シート名 | 短縮形 |
|---------|--------|
| マーブルビーチサイド | マーブル |
| アウトレットサイド | アウトレット |
| テーブル席 | テーブル |
| テーブル席（追加） | テーブル追加 |

---

### Excel エクスポート（`excel_exporter.py`）

**入力**: JSONデータ（`layout` + `seats` + `categories`）

**出力**: 座席区分の色が付いたExcelファイル

```python
def export_excel(data: dict, output_path: str) -> None:
    """
    openpyxlでセル背景色を設定。
    CATEGORIES の color（#RRGGBB）→ PatternFill に変換。
    チケット番号が採番済みの場合はセルに記載する。
    """
```

---

### PDF エクスポート（`pdf_exporter.py`）

**入力**: JSONデータ（`layout` + `seats` + `categories`）

**出力**: 座席表PDF

```python
def export_pdf(data: dict, output_path: str) -> None:
    """
    reportlab の canvas API でグリッドを描画。
    各セルに座席ID・チケット番号を描き、区分色で塗りつぶす。
    """
```

---

### ローカルHTTPサーバー（`server.py`）

フェーズ4でElectronから呼ばれる。`http.server` または Flask（軽量）で実装予定。

| エンドポイント | メソッド | 説明 |
|---------------|---------|------|
| `/import-excel` | POST | Excelファイルを受け取り `layout` JSONを返す |
| `/export-excel` | POST | JSONを受け取り Excelファイルを返す |
| `/export-pdf`   | POST | JSONを受け取り PDFファイルを返す |

---

## Phase 3 — Electron統合（`phase3_electron/`）※未実装

### 予定ファイル構成

```
phase3_electron/
├── main.js          Electronメインプロセス
├── preload.js       contextBridge（Node API露出を最小化）
├── package.json
└── assets/          アイコン等
```

### Python子プロセス管理（`main.js`）

```js
// Pythonサーバーをアプリ起動時に子プロセスとして起動
const { spawn } = require('child_process');
const pyServer = spawn('python', ['../phase2_python/server.py']);

// アプリ終了時に確実にkill
app.on('before-quit', () => pyServer.kill());
```

### BrowserWindow設定

`phase1_ui/index.html` をそのままロードする。
Excelインポート・出力ボタンのみ、`preload.js` 経由で `ipcRenderer` を使いPython APIを呼ぶ追加実装が必要。

---

## UIシート切り替え方針

- **現状**: シートごとにタブ切り替えで表示（シート名=タブ名）
- **備考（将来）**: 全シートを1画面に出して無限スクロールする要望が出た場合に対応を検討。横最大891列という規模から、DOM外セルを描画しない仮想スクロールが必要になる見込み。

---

## 確定済み事項（更新）

| 項目 | 内容 |
|------|------|
| Excelフォーマット | `docs/03_Excelフォーマット解析.md` に記載済み |
| 座席区分 | 販売チャネル（楽天/ぴあ/KKDAY/事務局/HIS/町民割/ツアー/ふるさと納税）= `CATEGORIES` |
| 座席ID形式 | `{シート短縮名}_{ブロック名}_{列番号}列_{座席番号}番`（例: `マーブル_赤31_1列_10番`） |
| チャネル色 | Excelの凡例セル背景色をそのまま使用（`docs/03_Excelフォーマット解析.md` 参照） |

## 残未確定事項

| 項目 | 影響箇所 | 確定後の作業 |
|------|---------|-------------|
| チケット番号フォーマットルール | `assignTickets()` の採番ロジック | 現状 `{prefix}{3桁連番}` 固定 |
